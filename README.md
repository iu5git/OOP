# Домашняя работа по курсу "Программирование на основе классов и шаблонов" 2 семестра кафедры ИУ5 МГТУ им Н.Э. Баумана.<!-- omit in toc -->

## Содержание <!-- omit in toc -->

- [Цели и задачи](#цели-и-задачи)
- [Шаблонный класс `Container<T>`](#шаблонный-класс-containert)
  - [Требования к классу `Container`:](#требования-к-классу-container)
- [Шаблонный класс `Database<T>`](#шаблонный-класс-databaset)
  - [Требования к классу `Database`:](#требования-к-классу-database)
- [Классы для хранения](#классы-для-хранения)
- [Демонстрация работы](#демонстрация-работы)

## Цели и задачи

Освоить разработку шаблонных классов, работу с динамической памятью и применение стандартных алгоритмов STL.

>*Разрешено использование алгоритмов STL (std::sort, std::find, std::copy_if и др.),*
>*но **запрещено** использование контейнеров STL (vector, list и т.д.).*
## Шаблонный класс `Container<T>`

Реализуйте шаблонный класс-контейнер для хранения набора объектов. Внутри должен храниться динамический массив, который автоматически расширяется при добавлении
новых элементов и может сжиматься при удалении.

### Требования к классу `Container`:

- Конструкторы:
    - по умолчанию (создаёт пустой контейнер);
    - копирования;
    - перемещения;
- Операторы присваивания (копирующий и перемещающий);
- Деструктор (освобождает память);
- `operator[]` — доступ по индексу (с проверкой границ, при ошибке — исключение или сообщение);
- `operator==` — сравнение двух контейнеров (поэлементно);
- `operator<` — лексикографическое сравнение (как у строк);
- `push(const T& value)` — добавление элемента в конец;
- `pop()` — удаление последнего элемента;
- `delete_at(size_t index)` — удаление элемента по индексу;
- `clear()` — полная очистка контейнера;
- `resize()` - должен увеличивать capacity при переполнении и уменьшать при
необходимости. `resize()` вызывается автоматически:
  - при переполнении (capacity увеличивается, например, в 2 раза);
  - при значительном уменьшении размера (например, если size < capacity / 4);
- `Iterator begin()`;
- `Iterator end()`;
- Операторы `<<` и `>>` для вывода в поток и ввода из потока.

Для сильных студентов - реализовать свой итератор в классе `Container`.
Допускается использование указателей как итераторов (`T*`).

## Шаблонный класс `Database<T>`

Класс-обёртка над `Container<T>`, который добавляет логику работы с файлами и
сортировку.

### Требования к классу `Database`:

- Правило пяти (все специальные методы);
- `read_from_file(filename)` — чтение данных из файла;
- `write_to_file(filename)` — запись данных в файл;
- `sort_qsort(comparator)` — сортировка с использованием собственной реализации быстрой сортировки (`quicksort` с шаблонным функтором);
- `find(value)` — поиск элемента с использованием `std::find`. Возвращает *индекс* элемента или -1, если элемент не найден;
- `add(value)` — добавление элемента;
- `remove_at(index)` — удаление по индексу;
- `remove_last()` — удаление последнего;
- `clear()` — очистка;
- `display()` — вывод на экран;
- `input_from_console()` — ввод с клавиатуры;

Каждый метод `Database` должен вызывать соответствующий метод `Container` (реализация не дублируется)

- `add()` → `push()`;
- `remove_last()` → `pop()`;
- `remove_at()` → `delete_at()`.

## Классы для хранения

Вам нужно продемонстрировать работу `Database` с двумя типами данных:

- **Класс `Planet`** (описан ниже)
- **Ваш класс из 3-й лабораторной работы**

**Класс `Planet`**

Поля:

- название (строка);
- диаметр (вещественное число);
- наличие жизни (булевский флаг);
- количество спутников (целое число);

Реализовать:

- конструкторы (по умолчанию, копирования, перемещения);
- операторы присваивания;
- `operator==`
- `operator<` (сравнение по названию);
- корректную работу с `bool` (вывод: yes/no или true/false);
- операторы `<<` и `>>` для ввода/вывода;
- проверку ввода;

## Демонстрация работы

В функции RunApplication() продумать вызов меню, где обеспечить возможность:

1. Создать **`Database<Planet>`** и **`Database<MyClass>`**.
2. Добавить несколько элементов вручную.
3. Вывести содержимое.
4. Продемонстрировать сортировку (обоими методами).
5. Сформировать новую базу данных, скопировав элементы из существующей с помощью `std::copy_if` и `лямбда`-выражения.
6. Продемонстрировать поиск элемента.
7. Сохранить данные в файл.
8. Очистить базу, прочитать из файла и снова вывести (проверка сохранения).
